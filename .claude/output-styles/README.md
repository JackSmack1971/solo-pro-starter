# Output Styles

Output styles are optional prompt overlays that shape response tone and structure without replacing repository rules or verification requirements.

Keep styles short, explicit, and compatible with the project's evidence standards.
